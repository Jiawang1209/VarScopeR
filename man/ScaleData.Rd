% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ScaleData.R
\name{ScaleData}
\alias{ScaleData}
\title{Scale feature abundance data in VarScope object}
\usage{
ScaleData(
  object,
  assay = "norm",
  new.assay = "scale",
  features = NULL,
  do.center = TRUE,
  do.scale = TRUE
)
}
\arguments{
\item{object}{A \code{VarScope} object containing normalized data.}

\item{assay}{Character. Name of the input assay to be scaled (default: "norm").}

\item{new.assay}{Character. Name of the scaled data (default: "scale").}

\item{features}{Character vector or NULL. Features to scale. If NULL, uses
all features or variable features if available.}

\item{do.center}{Logical. Whether to center the data (subtract mean)
(default: TRUE).}

\item{do.scale}{Logical. Whether to scale the data (divide by standard
deviation) (default: TRUE).}
}
\value{
A \code{VarScope} object containing the scaled data stored in
  \code{object$assays[[new.assay]]}. Scaling parameters (means and standard
  deviations) are stored in \code{object$misc$ScaleData}.
}
\description{
Scale (z-score standardize) feature abundance data by centering and scaling
each feature to have zero mean and unit variance. This is typically performed
after normalization and before PCA to ensure all features are on the same scale.
}
\examples{
\dontrun{
vsobj <- CreateVarScopeObject(
  counts = "inst/extdata/otutab.txt",
  meta.data = "inst/extdata/group.csv"
)
vsobj <- NormalizeData(vsobj, normalization.method = "log2")
vsobj <- FindVariableFeatures(vsobj)
vsobj <- ScaleData(vsobj)
}
}
